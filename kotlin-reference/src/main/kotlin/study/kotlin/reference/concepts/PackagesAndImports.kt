package study.kotlin.reference.concepts

fun main() {
    // 패키지를 지정하지 않으면 이름이 없는 default package가 지정된다

    // 일부 패키지는 import 없이 기본적으로 적용된다
    // println() (kotlin.io)을 사용할 때, 별도의 import 없이도 사용가능한 것은 이 때문이다.

    // kotlin.*

}

